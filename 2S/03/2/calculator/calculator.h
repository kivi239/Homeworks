const int error = (int)(-1e9);

///Class which is used to calculate the result of operation on two numbers
class Calculator
{
public:
  ///Method which calculates the result of operation on two numbers
  double calc(int value1, int value2, char operation);
};
